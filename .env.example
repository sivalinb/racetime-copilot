# Optional external tracing for npm run eval. No key required for the app.
LANGSMITH_TRACING=false
LANGSMITH_PROJECT=racetime-copilot
LANGSMITH_API_KEY=
# Gemini integration is deferred; no video provider is called in this build.
